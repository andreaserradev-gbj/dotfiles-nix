# herdr — terminal workspace manager for coding agents.
#
# Nix-managed copy (modules/home/herdr.nix symlinks it into place). Originally
# proven during the Phase 0 ephemeral trial (2026-09): keymap verified live,
# `herdr config check` passed, strace showed zero network connects.
#
# Keymap mirrors zellij's pane navigation, split across two families:
# - prefix chords keep zellij's alt-direct letters (prefix+alt+h…),
# - the bare singles are alt+ARROWS, mirroring zellij's Alt left/down/up/right
#   focus binds. The bare alt+h/j/k/l singles were dropped because neovim
#   claims alt+h (unhide in grep/fzf pickers); alt+arrows leave hjkl to the
#   app inside the pane. Verified against herdr 0.9.0 docs: arrow key names
#   are valid key strings.
# alt+d for split_horizontal mirrors zellij's pane-mode "d" (NewPane "down")
# — a stacked split, exactly what alt+d does in zellij's pane mode.
# Tab nav moved off alt+i/alt+o (2026-09-16): neovim claims alt+i, and zellij
# already passes alt+i through to apps in locked mode (config/zellij/config.kdl
# unbinds it there). Shift+arrows keep the arrows-for-navigation family and
# match the browser/VS Code convention.
# No quit key exists in herdr — prefix+q detaches; closing the server is a
# CLI action, not a keybinding (documented gap, doc/herdr.md).
onboarding = false

# The only phone-home knob. strace-verified: false → zero AF_INET connects.
# The binary itself cannot self-update under Nix (the store path is read-only);
# version bumps are the tool-pins.json entry, bumped by nfb (doc/herdr.md).
[update]
version_check = false

[theme]
name = "catppuccin"

[keys]
focus_pane_left  = ["prefix+alt+h", "alt+left"]
focus_pane_down  = ["prefix+alt+j", "alt+down"]
focus_pane_up    = ["prefix+alt+k", "alt+up"]
focus_pane_right = ["prefix+alt+l", "alt+right"]
split_vertical   = ["alt+n"]
split_horizontal = ["alt+d"]
zoom             = ["alt+f"]
new_tab          = ["alt+t"]
next_tab         = ["alt+shift+right"]
previous_tab     = ["alt+shift+left"]